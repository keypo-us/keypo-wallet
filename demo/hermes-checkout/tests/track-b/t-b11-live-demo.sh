#!/bin/bash
# T-B11 — Live demo (real card, only run once for final demo).
# Placeholder that prints SKIP.

echo "T-B11: Live demo with real card"
echo "  SKIP: final demo only, requires real card in vault"
