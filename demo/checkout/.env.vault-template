# Non-sensitive config (open tier)
PORT=8080
DB_USERNAME=checkout
DB_PASSWORD=localdev
DB_NAME=checkout_demo
DB_PORT=5432
DB_HOST=localhost
NODE_ENV=local

# Card secrets (biometric tier in production, open tier during Phase 2)
# The agent must NEVER populate these fields.
CARD_NUMBER=
NAME_ON_CARD=
EXPIRATION_MONTH=
EXPIRATION_YEAR=
SECURITY_CODE=
